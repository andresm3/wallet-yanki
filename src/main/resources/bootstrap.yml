spring:
  application:
    name: wallet-yanki
  cloud:
    config:
      uri: http://localhost:7000

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka